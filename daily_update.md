# 일일 수집 런북

**하루에 여러 번 실행된다.** 07:37을 시작으로 3시간마다, 19:37까지 다섯 번이다.
일간은 그날 나온 기사를 실행할 때마다 쌓아가는 방식이라, 아침에는 몇 건이던 것이
저녁에는 그날치가 다 차 있게 된다.

프로젝트 루트는 이 파일이 있는 폴더다.

## 0. 오늘 파일을 먼저 읽는다

```bash
python -X utf8 -c "
import json, glob
from datetime import datetime, timezone, timedelta
today = datetime.now(timezone(timedelta(hours=9))).date().isoformat()
try:
    doc = json.load(open(f'data/daily/{today}.json', encoding='utf-8'))
    print(f'{today} 기존 {doc[\"item_count\"]}건')
    for item in doc['items']:
        print(' ', item['url'])
except FileNotFoundError:
    print(f'{today} 파일 없음 - 첫 실행')
"
```

여기 나온 URL은 **이미 담긴 기사**다. 아래 절차에서 이 URL들은 건너뛰고
**새로 나온 기사만** 찾는다. 기존 항목은 지우거나 고치지 않는다.

## 1. 검색 — 네이버 뉴스가 기본이다

**`naver_news.js`를 Playwright(`browser_run_code_unsafe`의 `filename` 인자)로 실행한다.**

```
browser_run_code_unsafe(filename="<프로젝트 경로>/naver_news.js")
```

네이버 뉴스에 등록된 모든 언론사(news.naver.com/main/officeList.naver)가 검색 대상이라,
일반 웹 검색이 놓치는 매체까지 한 번에 훑는다. 실측 비교: 같은 날 같은 주제로
일반 웹 검색은 1건, 네이버는 39건을 찾았다. 더벨·비즈월드·딜라이트닷넷·뉴스브라이트·
FETV·데일리안 같은 매체는 일반 검색으로 거의 안 나온다.

결과는 `오늘기사`(당일)와 `참고_이전기사`로 나뉘어 나온다. **`오늘기사`만 쓴다.**
검색어를 늘리거나 바꾸려면 `naver_news.js`의 `QUERIES` 배열을 고친다.

`오늘` 건수가 0이면 셀렉터가 깨진 것일 수 있다. 네이버는 클래스명을 난독화해서
가끔 구조가 바뀐다. 이때는 페이지를 열어 구조를 확인하고 `naver_news.js`를 고친다.

### 보조 — 일반 웹 검색

네이버 결과가 빈약하거나 특정 주제를 파고들 때만 쓴다.

**제도·정책**
- `퇴직연금`
- `기금형 퇴직연금`
- `퇴직연금 의무화 사외적립`
- `연금개혁`

**계좌·상품**
- `IRP 개인형퇴직연금`
- `확정기여형 DC 퇴직연금`
- `확정급여형 DB 퇴직연금`
- `퇴직연금 디폴트옵션`
- `퇴직연금 TDF ETF`

**자금 흐름·수익률**
- `퇴직연금 적립금 순유입 순유출`
- `퇴직연금 수익률 공시`
- `퇴직연금 실물이전`

**법인·기업 운용**
- `DB형 적립금 운용 IPS 적립금운용계획서`
- `퇴직연금 OCIO 위탁운용`
- `기업 퇴직연금 도입 컨설팅`

**사업자**
- `퇴직연금 사업자 은행 증권 인터뷰`
- `퇴직연금 점유율 적립금 순위`

### 매일경제(mk.co.kr) 기사

WebFetch가 막혀 있다. 브라우저(Playwright)로 열어 본문을 읽는다.

```
page.goto(url) → .news_cnt_detail_wrap 의 innerText, .time_area 의 게재일
```

## 2. 선별

**일간은 기준일 당일에 게재된 기사만 담는다.** 과거 기사나 배경 이슈로 빈자리를 채우지 않는다.
**최대 20건**까지 담되, 안 되면 안 되는 대로 둔다 — 다음 실행에서 채워진다.

**0번에서 확인한 URL은 다시 담지 않는다.** 새 기사만 골라 기존 목록 뒤에 더한다.
새 기사가 한 건도 없으면 5번(저장)을 건너뛰고 6번(빌드)으로 간다.
오늘 파일이 아직 없고 새 기사도 없으면 아무것도 하지 않고 끝낸다.

이미 담긴 기사의 후속 보도가 나왔다면, 그 후속 기사도 새 항목으로 담는다.
같은 사건인지는 `rollup.py`가 제목 유사도로 판단해 주간 이상에서 묶어 준다.

(주간·월간·연간은 `rollup.py`가 누적된 일간 파일에서 TOP 20을 뽑으므로 여기서 신경 쓰지 않는다.
상한을 바꾸려면 `rollup.py`의 `TOP_N` 하나만 고치면 된다.)

**카테고리 5종** — 각 기사에 하나씩 붙인다. 값은 정확히 이 문자열이어야 한다.

| 값 | 담는 것 | 예 |
|---|---|---|
| `제도·규제` | 법 개정, 감독당국 가이드라인, 정부 제도 개편 방향 | 정부, 하반기 디폴트옵션 손질 |
| `업계·경쟁사` | 사업자의 점유율·순위, 신상품 출시, 수수료 경쟁, 경영진 인터뷰 | 하나은행 부행장 인터뷰, 적립금 53조 |
| `운용·상품` | 시장 전체의 자금 흐름과 수익률, 상품군 동향, 자산배분 제언 | 디폴트옵션 첫 순유출, 2분기 1.4조 감소 |
| `법인·자금운용` | 개별 기업·법인의 적립금 운용, IPS, OCIO·위탁운용, 사외적립, 기금형 운용주체 | NH투자증권, DB 적립금 IPS 컨설팅 강화 |
| `가입자·사회` | 개인 가입자의 수령·인출, 고령화·노후설계, 국민연금·기초연금 | 기초연금 수급률 4년 연속 하락 |

**걸칠 때 가르는 법**

- `운용·상품` vs `법인·자금운용` — **시장 전체**의 흐름이면 `운용·상품`,
  **특정 기업 고객**의 자금을 어떻게 굴리는지면 `법인·자금운용`.
- `운용·상품` vs `제도·규제` — 수치·자금흐름이 본문의 중심이면 `운용·상품`,
  제도가 어떻게 바뀌는지가 중심이면 `제도·규제`.
- `업계·경쟁사` vs `법인·자금운용` — 사업자끼리의 경쟁 구도면 `업계·경쟁사`,
  그 사업자가 기업 고객 자금을 다루는 방식이면 `법인·자금운용`.

**균형** — 기사가 충분하면 다섯 카테고리에서 각각 최소 1건을 포함한다. 당일 기사가 적으면
억지로 맞추지 않는다.

**중요도(`importance`, 1~5)** — 5는 제도 확정·대형 발표처럼 업무에 직접 영향을 주는 건,
1은 참고용 단신.

**제외** — 광고성 기사, 특정 상품 홍보, 블로그·정보성 콘텐츠 페이지(날짜가 없는 것),
같은 사건의 단순 중복 보도(가장 충실한 1건만 남긴다).

## 3. 게재일 확인

**URL에서 날짜가 확실히 읽히지 않는 기사는 반드시 원문을 열어 게재일을 확인한다.**
날짜를 추측해서 적지 않는다. 확인이 불가능하면 그 기사는 제외한다.

날짜가 URL에 드러나는 예: `fnnews.com/news/202609071643...` → 2026-09-07

## 4. 요약

각 기사를 **3~4문장**으로 요약한다.

- 기사에 실제로 있는 사실만 쓴다. 추측이나 배경 설명을 덧붙이지 않는다.
- 숫자(금액, 비율, 시행일)는 기사에 나온 그대로 옮긴다.
- 문장은 평서형으로 끝낸다.

## 5. 저장

`data/daily/YYYY-MM-DD.json`에 아래 형식으로 저장한다. 파일이 이미 있으면
**기존 `items` 뒤에 새 기사를 이어 붙인 전체 목록**을 다시 쓴다.
`items`는 중요도 내림차순으로 정렬한다.

```json
{
  "period": "daily",
  "key": "2026-09-10",
  "label": "2026-09-10 (목)",
  "generated_at": "2026-09-10T07:40:00+09:00",
  "item_count": 10,
  "shortfall_note": null,
  "items": [
    {
      "id": "a1b2c3d4e5f6",
      "title": "기사 제목",
      "summary": "요약 문장 하나. 요약 문장 둘. 요약 문장 셋.",
      "category": "제도·규제",
      "importance": 4,
      "source": "언론사명",
      "published": "2026-09-10",
      "url": "https://..."
    }
  ]
}
```

`id`와 `label`은 아래로 얻는다.

```bash
python -X utf8 -c "import dedupe; print(dedupe.make_id('여기에 기사 URL'))"
python -X utf8 -c "import periods; print(periods.label_for('daily', '2026-09-10'))"
```

일간이 20건이 안 되는 것은 정상이다. 특히 오전에는 그날 기사가 아직 안 나왔을 뿐이다.
`shortfall_note`에는 그 사정을 한 문장으로 적는다
(예: `오늘 지금까지 나온 관련 기사는 3건입니다. 갱신될수록 늘어납니다.`).
20건을 채웠으면 `null`.
유료 기사나 로그인이 필요한 매체는 `source` 끝에 ` (유료)`를 붙인다.

## 6. 집계와 빌드

```bash
python -X utf8 rollup.py
python -X utf8 build.py
```

`rollup.py`는 오늘이 속한 주·월·연 파일을 다시 만든다. 같은 사건을 다룬 기사는
제목 유사도로 묶여 대표 1건 + `관련 기사`로 접힌다.

## 7. 검토

`data/weekly/`, `data/monthly/`, `data/yearly/`의 오늘자 파일을 열어 확인한다.

- 같은 사건이 두 항목으로 갈라져 있으면, 한쪽을 지우고 다른 쪽 `related`에 옮긴다.
- 서로 다른 사건이 잘못 묶였으면, 묶인 기사를 별도 항목으로 분리한다.
- 순위가 실제 중요도와 어긋나면 `items` 순서를 직접 바꾼다.
- 고쳤으면 `python -X utf8 build.py`를 다시 실행한다.

검증:

```bash
python -X utf8 -c "
import store, glob
for path in sorted(glob.glob('data/*/*.json')):
    errors = store.doc_errors(store.load(path))
    print(path, '정상' if not errors else errors)
"
```

## 8. 실패했을 때

검색이 실패하거나 관련 기사를 하나도 못 찾았으면, **아무 파일도 쓰지 않고 중단한다.**
`index.html`은 마지막으로 성공한 데이터를 그대로 보여준다.

## 9. 게시 — 여기서는 하지 않는다

**게시는 `publish.ps1`이 맡는다.** `run_daily.ps1`이 이 절차를 마친 뒤 자동으로 부른다.
6번의 `build.py`까지 끝내고 7번 검토를 마쳤으면 여기서 멈추면 된다.

이렇게 나눈 이유: `claude -p` 헤드리스 세션에는 `Artifact` 도구가 주입되지 않는다.
`--allowedTools`에 명시해도 마찬가지다. 그래서 2026-09-10~11 이틀간 로컬 파일은
정상 갱신됐는데 웹 페이지만 멈춰 있었다. 게시처럼 반드시 성공해야 하는 단계는
도구 유무에 좌우되지 않는 결정적인 스크립트가 맡는 편이 안전하다.

`publish.ps1`은 `git add` → `commit` → `push`를 한다. 푸시되면 GitHub Pages가
1분 안에 다시 빌드해 아래 주소에 반영한다.

```
https://pakvermars.github.io/pension-issues-dashboard/
```

수동으로 게시해야 하면 프로젝트 루트에서 `pwsh -File publish.ps1`을 실행한다.
