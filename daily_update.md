# 일일 수집 런북

**하루에 여러 번 실행된다.** 07:37을 시작으로 3시간마다, 19:37까지 다섯 번이다.
일간은 그날 나온 기사를 실행할 때마다 쌓아가는 방식이라, 아침에는 몇 건이던 것이
저녁에는 그날치가 다 차 있게 된다.

프로젝트 루트는 이 파일이 있는 폴더다.

## 0. 오늘 파일을 먼저 읽는다

```bash
python -X utf8 -c "
import json, glob
from datetime import datetime, timezone, timedelta
today = datetime.now(timezone(timedelta(hours=9))).date().isoformat()
try:
    doc = json.load(open(f'data/daily/{today}.json', encoding='utf-8'))
    print(f'{today} 기존 {doc[\"item_count\"]}건')
    for item in doc['items']:
        print(' ', item['url'])
except FileNotFoundError:
    print(f'{today} 파일 없음 - 첫 실행')
"
```

여기 나온 URL은 **이미 담긴 기사**다. 아래 절차에서 이 URL들은 건너뛰고
**새로 나온 기사만** 찾는다. 기존 항목은 지우거나 고치지 않는다.

## 1. 검색

오늘 날짜(KST) 기준으로 다음 질의를 각각 웹 검색한다. 오늘·어제 기사를 우선한다.

- `퇴직연금`
- `IRP 개인형퇴직연금`
- `확정기여형 DC 퇴직연금`
- `확정급여형 DB 퇴직연금`
- `퇴직연금 디폴트옵션`
- `퇴직연금 적립금`
- `퇴직연금 수익률`
- `기금형 퇴직연금`
- `연금개혁`
- `퇴직연금 TDF`

## 2. 선별

**일간은 기준일 당일에 게재된 기사만 담는다.** 과거 기사나 배경 이슈로 빈자리를 채우지 않는다.
10건이 안 되면 안 되는 대로 둔다 — 다음 실행에서 채워진다.

**0번에서 확인한 URL은 다시 담지 않는다.** 새 기사만 골라 기존 목록 뒤에 더한다.
새 기사가 한 건도 없으면 5번(저장)을 건너뛰고 6번(빌드)으로 간다.
오늘 파일이 아직 없고 새 기사도 없으면 아무것도 하지 않고 끝낸다.

이미 담긴 기사의 후속 보도가 나왔다면, 그 후속 기사도 새 항목으로 담는다.
같은 사건인지는 `rollup.py`가 제목 유사도로 판단해 주간 이상에서 묶어 준다.

(주간·월간·연간은 `rollup.py`가 누적된 일간 파일에서 TOP 10을 뽑으므로 여기서 신경 쓰지 않는다.)

**카테고리 4종** — 각 기사에 하나씩 붙인다. 값은 정확히 이 문자열이어야 한다.

- `제도·규제` — 법 개정, 금감원·고용노동부 가이드라인, 디폴트옵션 등 제도 변화
- `업계·경쟁사` — 은행·증권·보험사의 점유율, 신상품·서비스, 수수료 경쟁, 로보어드바이저
- `가입자·사회` — 중도인출, 연금화 수령, 고령화·노후설계, 국민연금·기초연금 등
- `법인·자금운용` — 기업·법인 고객의 퇴직연금 도입과 운용, 사외적립, 기금형 운용주체
  (수탁법인·국민연금공단 참여 논쟁), OCIO·위탁운용사 선정, 법인 자금운용·자산배분

한 기사가 둘에 걸치면 **자금이 누구 손에서 어떻게 굴러가는지**가 핵심이면 `법인·자금운용`,
제도 자체의 변화가 핵심이면 `제도·규제`를 고른다.

**균형** — 기사가 충분하면 네 카테고리에서 각각 최소 1건을 포함한다. 당일 기사가 적으면
억지로 맞추지 않는다.

**중요도(`importance`, 1~5)** — 5는 제도 확정·대형 발표처럼 업무에 직접 영향을 주는 건,
1은 참고용 단신.

**제외** — 광고성 기사, 특정 상품 홍보, 블로그·정보성 콘텐츠 페이지(날짜가 없는 것),
같은 사건의 단순 중복 보도(가장 충실한 1건만 남긴다).

## 3. 게재일 확인

**URL에서 날짜가 확실히 읽히지 않는 기사는 반드시 원문을 열어 게재일을 확인한다.**
날짜를 추측해서 적지 않는다. 확인이 불가능하면 그 기사는 제외한다.

날짜가 URL에 드러나는 예: `fnnews.com/news/202609071643...` → 2026-09-07

## 4. 요약

각 기사를 **3~4문장**으로 요약한다.

- 기사에 실제로 있는 사실만 쓴다. 추측이나 배경 설명을 덧붙이지 않는다.
- 숫자(금액, 비율, 시행일)는 기사에 나온 그대로 옮긴다.
- 문장은 평서형으로 끝낸다.

## 5. 저장

`data/daily/YYYY-MM-DD.json`에 아래 형식으로 저장한다. 파일이 이미 있으면
**기존 `items` 뒤에 새 기사를 이어 붙인 전체 목록**을 다시 쓴다.
`items`는 중요도 내림차순으로 정렬한다.

```json
{
  "period": "daily",
  "key": "2026-09-10",
  "label": "2026-09-10 (목)",
  "generated_at": "2026-09-10T07:40:00+09:00",
  "item_count": 10,
  "shortfall_note": null,
  "items": [
    {
      "id": "a1b2c3d4e5f6",
      "title": "기사 제목",
      "summary": "요약 문장 하나. 요약 문장 둘. 요약 문장 셋.",
      "category": "제도·규제",
      "importance": 4,
      "source": "언론사명",
      "published": "2026-09-10",
      "url": "https://..."
    }
  ]
}
```

`id`와 `label`은 아래로 얻는다.

```bash
python -X utf8 -c "import dedupe; print(dedupe.make_id('여기에 기사 URL'))"
python -X utf8 -c "import periods; print(periods.label_for('daily', '2026-09-10'))"
```

일간이 10건이 안 되는 것은 정상이다. 특히 오전에는 그날 기사가 아직 안 나왔을 뿐이다.
`shortfall_note`에는 그 사정을 한 문장으로 적는다
(예: `오늘 지금까지 나온 관련 기사는 3건입니다. 갱신될수록 늘어납니다.`).
10건을 채웠으면 `null`.
유료 기사나 로그인이 필요한 매체는 `source` 끝에 ` (유료)`를 붙인다.

## 6. 집계와 빌드

```bash
python -X utf8 rollup.py
python -X utf8 build.py
```

`rollup.py`는 오늘이 속한 주·월·연 파일을 다시 만든다. 같은 사건을 다룬 기사는
제목 유사도로 묶여 대표 1건 + `관련 기사`로 접힌다.

## 7. 검토

`data/weekly/`, `data/monthly/`, `data/yearly/`의 오늘자 파일을 열어 확인한다.

- 같은 사건이 두 항목으로 갈라져 있으면, 한쪽을 지우고 다른 쪽 `related`에 옮긴다.
- 서로 다른 사건이 잘못 묶였으면, 묶인 기사를 별도 항목으로 분리한다.
- 순위가 실제 중요도와 어긋나면 `items` 순서를 직접 바꾼다.
- 고쳤으면 `python -X utf8 build.py`를 다시 실행한다.

검증:

```bash
python -X utf8 -c "
import store, glob
for path in sorted(glob.glob('data/*/*.json')):
    errors = store.doc_errors(store.load(path))
    print(path, '정상' if not errors else errors)
"
```

## 8. 실패했을 때

검색이 실패하거나 관련 기사를 하나도 못 찾았으면, **아무 파일도 쓰지 않고 중단한다.**
`index.html`은 마지막으로 성공한 데이터를 그대로 보여준다.

## 9. 게시

`build.py`가 만든 **`artifact.html`**(웹 게시용 조각)을 Artifact로 재게시한다.
`index.html`이 아니다 — 그건 로컬 더블클릭용 전체 문서다.

반드시 기존과 **같은 URL**로 업데이트한다. `Artifact` 도구의 `url` 파라미터에 아래 주소를 넘긴다.

```
https://claude.ai/code/artifact/193698ba-0ed1-4069-8493-efd6cc70b3c2
```

favicon은 `📊`로 고정한다.
